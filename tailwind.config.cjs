const config = {
	mode: 'jit',
	purge: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		constainer: {
			center: true,
			padding: '2rem'
		},
		extend: {}
	},
	plugins: []
};

module.exports = config;
